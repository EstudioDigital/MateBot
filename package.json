{
  "name": "nomi-bot",
  "version": "1.0.0",
  "description": "El socio digital de tu negocio por WhatsApp",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "start:all": "concurrently \"npm run dev\" \"npm run dev --prefix panel\" \"npm run dev --prefix landing\"",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "@fastify/jwt": "^8.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "@prisma/client": "^5.14.0",
    "bcrypt": "^6.0.0",
    "bullmq": "^5.8.0",
    "fastify": "^4.27.0",
    "ioredis": "^5.3.2",
    "openai": "^4.104.0",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "pino-pretty": "^11.0.0",
    "prisma": "^5.14.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
